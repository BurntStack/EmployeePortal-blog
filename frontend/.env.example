# Base URL of the employee portal backend's API
VITE_API_URL=http://localhost:8000/api

# Google OAuth Client ID (Google Cloud Console -> OAuth consent screen ->
# Web application credential, with this app's origin under "Authorized
# JavaScript origins"). Login is disabled entirely until this is set.
VITE_GOOGLE_CLIENT_ID=
